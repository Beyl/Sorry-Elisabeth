#pragma once

/**
 * Define the four possible directions of the objects in the game
 * 
 * Used in various functions and classes in the project.
 */
enum class Direction : int {
	LEFT, RIGHT, FRONT, BACK
};